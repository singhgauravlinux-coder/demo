apiVersion: apps/v1
kind: Deployment
metadata:
  name: language-service
  namespace: bakery
  labels:
    app: language-service
    app.kubernetes.io/part-of: crumb-and-ember
spec:
  replicas: 1
  selector:
    matchLabels:
      app: language-service
  template:
    metadata:
      labels:
        app: language-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: language-service
          image: ghcr.io/singhgauravlinux-coder/demo-bakery/language-service:4e7ca01
          imagePullPolicy: Always
          ports:
            - containerPort: 3023
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: SERVICE_NAME
              value: "language-service"
            - name: PORT
              value: "3023"
            - name: LOG_LEVEL
              value: "info"
          readinessProbe:
            httpGet:
              path: /ready
              port: 3023
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3023
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: language-service
  namespace: bakery
  labels:
    app: language-service
spec:
  selector:
    app: language-service
  ports:
    - port: 3023
      targetPort: 3023
      protocol: TCP
